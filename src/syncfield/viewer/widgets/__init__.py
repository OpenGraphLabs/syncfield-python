"""Viewer widget modules.

Each module owns one logical section of the viewer UI. The top-level
:class:`syncfield.viewer.widgets.layout.ViewerLayout` composes them into
the complete screen and drives per-frame updates from a
:class:`SessionSnapshot`.
"""
